# DCAP Multi-Version Configuration
# This file defines supported deployment versions

[metadata]
# Current active version - Foundry writes to deployment/current/
current = "v1.1"

# All supported versions (including current)
supported = ["v1.0", "v1.1"]

# Version 1.0 Configuration
[versions.v1_0]
name = "v1.0"
description = "Legacy deployment version with non-versioned DAOs"
# Historical deployments can be loaded from URL or local copy
deployment_url = "https://github.com/automata-network/automata-dcap-attestation/tree/main/evm/deployment"

# Version 1.1 Configuration (CURRENT)
[versions.v1_1]
name = "v1.1"
description = "Current deployment version with versioned DAOs and TcbEvalDao support"
# Current version uses deployment/current/ directory (written by Foundry)