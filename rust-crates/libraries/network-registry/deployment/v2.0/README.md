# V2 deployment registry

No V2 deployments are published yet. Populate chain directories only after verified deployment
and the real-proof release gate. Preserve legacy keys and add AutomataDcapAttestationFeeV2,
V3QuoteVerifierV2, V4QuoteVerifierV2, V5QuoteVerifierV2, and PCKHelperV2.
Never resolve a missing V2 address to the old Fee contract.

The v1.1 directory is the frozen pre-V2 snapshot. The current alias is not promoted by this change.
